DATABASE_URL="postgresql://postgres:change-me@127.0.0.1:5432/khan_sofa?schema=public"
# Optional connection used only by `npm run db:create`. It must point to an
# existing database and use a role allowed to create databases.
DATABASE_ADMIN_URL="postgresql://postgres:change-me@127.0.0.1:5432/postgres"
JWT_SECRET="change-this-to-a-secure-random-string"
SESSION_SECRET="change-this-to-a-secure-random-string"
PORT=4000
HOST=0.0.0.0
FRONTEND_URL="http://localhost:3000"
CORS_ORIGIN="http://localhost:3000"
STRIPE_PUBLISHABLE_KEY="pk_test_your_key_here"
STRIPE_SECRET_KEY="sk_test_your_key_here"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"

# Hostinger mailbox credentials (the password used for webmail).
SMTP_HOST="smtp.hostinger.com"
SMTP_PORT=465
SMTP_USER=""
SMTP_PASSWORD=""
EMAIL_FROM_NAME="Khan Sofa"

# Log sanitized HTTP requests and JSON responses for debugging.
HTTP_LOGGING=true
